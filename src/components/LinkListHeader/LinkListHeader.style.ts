import styled from 'styled-components'

export const LinkListHeaderWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 2.4rem 0;
  border-bottom: 2px solid ${(props) => props.theme.color.gray200};
  box-sizing: border-box;
  margin-bottom: 1.6rem;
`
